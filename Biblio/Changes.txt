Take these files:
---
**New features**
Main page view: 
- In the three dots dropdown add the option to summarize the book, this will either use the local llm or a quick search in with the format Summary of {book title, book author} in their search engine.
Reader View:
- When the audio icon is clicked, change it so that instead of files being pulled up add an audio playback system under the page that says there is no tts or audio playback downloaded if there is no tts or audioplayback detected.
- Within a page where notes are added add a bookmark icon next to the beginning of the text where the note was made originally, it should responsively update in real-time and be within the margin instead of displacing the text.
- Change the jump to page button to be in the {page #} format and have that number be a button where when clicked it takes you to the page, this should be directly to the left of the date the note was made, when clicked it should jump you to that page with the selected text highlighted.
- When words are highlighted the user should be given the opption to highlight the words, summarize, add note, or play audio.
Settings:
- Add a times read listing next to the amount of pages read within the most read books section.

---
Bugs and Issues:
Main Page View:
- If the reader has no audiobooks downloaded show that along with the audio files that are accepted in the audiobooks tab.
Reader View:
- Make the settings in the reader view update quicker as sometimes they require the user to change pages in order to update.
Specifcally:
   - Two page spread
   - highlight word on hover
   - underline current line
- Fix the way that the Underline Current Line feature works so that it more accurately detects the line the user is on.
- Fix how the user looks up page numbers in the chapter search bar, it should show along with chapter search options just underneath.
ex. Search = 68
   - Result -> Chapter 68
   - Result -> Page 68
- Refer to a more accurate translator as it's translations are often wrong or irrelevant keep the context window.
- **Biggest Issues** Fix the way pages are rendered with words within the page viewport to dynamically adjust the amount of pages so that no words are clipping through the bottom and all pages are consistent in size. This should be quick in realtime so there is no lag when changing font sizes.
Settings:
- When no llm is loaded say that no llm is loaded
- When reloading the folder check for any duplicate epub files or title names so as to not duplicate books that are already loaded.

Maybe add these options later (More complex):
- Custom "Libraries" so that users can organize their books better either by genre or by tags or other custom options.
- Porting over books from other apps.
- Log-in and authentication.
- TTS integration for the text that can begin from specifc words when clicked on.